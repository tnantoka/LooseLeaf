<div class="topbar">
  <div class="fill">
    <div class="container">

      <a class="brand" href="<%= config.process.loc %>/"><%= config.site.title %></a>

      <form action="<%= config.process.loc %>/search/" id="searchForm">
        <input type="text" placeholder="keyword" name="keyword" id="keyword" value="<%= req.params.keyword || '' %>" />
        <button class="btn" type="submit">search</button>
      </form>

      <ul class="nav secondary-nav" id="loginLink">
        <li><a href="<%= config.process.loc %>/users">users</a></li>
        <% if (req.session.user) { %>
        <li><a href="#" id="openFiles">files</a></li>
        <% if (!/^\/users/.test(req.url)) { %>
        <li><a href="#" id="newpost">new post</a></li>
        <% } %>
        <% if (req.session.user.isAdmin && /^\/users/.test(req.url)) { %>
          <li><a href="#" id="newuser">new user</a></li>
        <% } %>
        <li class="dropdown" data-dropdown="dropdown" >
          <a href="#" class="dropdown-toggle"><%= req.session.user.username %></a>
          <ul class="dropdown-menu">
            <li><a href="<%= config.process.loc %>/users/<%= req.session.user.id %>">profile</a></li>
            <li class="divider"></li>
            <li><a href="<%= config.process.loc %>/auth/logout">logout</a></li>
          </ul>
        </li>
        <% } else {%>
        <li><a href="#" id="loginButton">login</a></li>
        <% } %>
      </ul>

      <form action="<%= config.process.loc %>/auth/login" method="post" class="pull-right" id="loginForm">
        <input class="input-small" type="text" name="user[username]" id="username" placeholder="Username">
        <input class="input-small" type="password" name="user[password]" placeholder="Password">
        <button class="btn" type="submit">login</button>
      </form>

    </div>
  </div>
</div>

